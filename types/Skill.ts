export type Skill = {
  _id: string;
  _createdAt: Date;
  name: string;
  slug: string;
  image: string;
  rating: string;
  // I could add this in the Sanity Studio, to make the text Portable
  // description: string;
};
