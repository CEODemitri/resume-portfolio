import project from "./project-schema";
import page from "./page-schema";
import contact from "./contact-schema";
import skill from "./skill-schema";

const schemas = [project, page, contact, skill];

export default schemas;
